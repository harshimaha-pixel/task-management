# Vite React Application Environment Configuration

# API Configuration
VITE_API_URL=http://localhost:3000/api

# Application Configuration
VITE_APP_NAME=Task Management
VITE_DEBUG=true

# Note: All variables must start with VITE_ to be accessible in the React app
# Access them with: import.meta.env.VITE_VARIABLE_NAME
